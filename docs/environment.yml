channels:
  - conda-forge
  - nodefaults

dependencies:
  # run deps
  - doit
  - python >=3.8,<3.12
  - tomli
  # run deps
  # optional deps
  - jinja2
  - json-e
  - jsonschema-with-format
  - pyyaml
  - jsonschema-gentypes
  # optional deps
  # build deps
  - flit >=3.8
  - pip
  # build deps
  # docs deps
  - docutils >=0.19
  - myst-nb
  - pydata-sphinx-theme
  - sphinx-autobuild
  - sphinx-autodoc-typehints
  - sphinx-copybutton
  - sphinx-design
  - sphinx-jsonschema
  - sphinxcontrib-mermaid
  # docs deps

name: doitoml-docs
